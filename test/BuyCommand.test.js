require("mocha");
const { assert } = require("chai");
const { BuyCommand } = require("../lib/BuyCommand");

describe.skip("BuyCommand", () => {
    it("execute", function (done) {
        done();
    });

    it("stop", function (done) {
        done();
    });

    it("stop", function (done) {
        done();
    });
});
